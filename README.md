# xuefei-wang.github.io

Personal academic website for Xuefei (Julie) Wang.

## Setup

1. Add a profile photo as `assets/profile.png`
2. Push to GitHub — the site will be live at `https://xuefei-wang.github.io`

## Local preview

Open `index.html` in a browser, or run a local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
